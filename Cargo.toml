[package]
name = "cloud"
version = "0.1.0"
edition = "2021"

[dependencies]

tokio = { version = "1.42.0", default-features = false, features = [
  "net",
  "rt-multi-thread",
  "time",
  "sync",
  "macros",
] }
